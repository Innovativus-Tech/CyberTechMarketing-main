# Cybertech Marketing

Cybertech Marketing's website, built with Next.js and React.

## Local development

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env.local` and provide `MONGODB_URI` to persist contact form submissions locally.
